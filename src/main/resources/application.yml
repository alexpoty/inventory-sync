spring:
  application:
    name: inventory-sync
  datasource:
    username: root
    password: root
    url: jdbc:postgresql://localhost:5432/inventory-sync
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    hibernate:
      ddl-auto: none